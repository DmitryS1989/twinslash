language: ruby
rvm:
 - 3.0.1
before_script:
  - cp config/database.yml.travis config/database.yml
  - psql -c 'create database db_test;' -U postgres
services:
  - postgresql
before_install:
  - gem update --system
  - gem --version